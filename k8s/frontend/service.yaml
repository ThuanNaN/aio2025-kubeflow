apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: yolo-app
  labels:
    app: frontend
spec:
  type: LoadBalancer
  selector:
    app: frontend
    tier: ui
  ports:
  - port: 80
    targetPort: 7860
    protocol: TCP
    name: http
